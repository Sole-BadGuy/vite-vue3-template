// 根据环境变量抛出的对应的路由文件
// import siteRouter, {
//   asyncRoutes as siteAsyncRoutes,
//   allRoutes as siteAllRoutes,
//   resetRouter as siteResetRouter,
//   routes as siteRoutes
// } from './site'
// import bossRouter, {
//   asyncRoutes as bossAsyncRoutes,
//   allRoutes as bossAllRoutes,
//   resetRouter as bossResetRouter,
//   routes as bossRoutes
// } from './boss'

// const router = import.meta.env.VITE_APP_PORT === 'boss' ? bossRouter : siteRouter
// const asyncRoutes = import.meta.env.VITE_APP_PORT === 'boss' ? bossAsyncRoutes : siteAsyncRoutes
// const allRoute = import.meta.env.VITE_APP_PORT === 'boss' ? bossAllRoutes : siteAllRoutes
// const resetRouter = import.meta.env.VITE_APP_PORT === 'boss' ? bossResetRouter : siteResetRouter
// const routes = import.meta.env.VITE_APP_PORT === 'boss' ? bossRoutes : siteRoutes
// export { asyncRoutes, allRoute, resetRouter, routes }
// export default router
